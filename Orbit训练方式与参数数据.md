# Orbit 训练方式与参数数据

> 归属：电脑版 Orbit 主线
> 用途：统一记录预训练、继续训练、微调、样本、Token、步数、参数推荐和内存估算

## 1. 训练方式

### 预训练（从零开始）

没有父 checkpoint 时，按所选 `orbit-hybrid-moe-v1` 配置随机初始化。训练
主体应使用经过授权、清洗、去重和验证集隔离的文献、教材、科学/数学资料、
技术资料和代码；少量结构化任务、对话和 Orbit 身份样本作为补充。

### 继续预训练（第 2～N 次）

选择上一轮 Orbit checkpoint，加载父权重，在新增语料上继续训练。父模型、
规模、配置和数据来源写入训练血缘；不能把继续训练显示成全新随机初始化。

### 微调

微调必须选择已有兼容父模型。可以选择本地 Orbit checkpoint，或选择经过
真实 tokenizer、架构、词表、权重格式和推理/训练适配验证的下载模型。外部
模型不能从零预训练；没有父模型时，界面和后端都必须阻止微调。

## 2. 计量单位

- 独立样本：一篇文献片段、一个结构化任务或一条有边界的训练示例。
- 字符：文本字符数量，不能直接当作 Token。
- UTF-8 字节：当前 Orbit byte-level 模型的内部训练 Token。
- 优化器步数：完成 Batch 和梯度累计后的一次参数更新。
- 最终优化器 Token 覆盖量：`步数 × 序列长度 × Batch × 梯度累计`。

例如：`2000 步 × 512 序列 × 1 Batch × 1 梯度累计 = 1,024,000 Token`。
一个样本不是一步；中文字符、英文字符和 UTF-8 字节也不能混为同一计量。

## 3. 规模规划参考

从随机权重训练基础模型时，使用每参数约 20 个训练 Token 的规划参考，
不是质量保证：

| 档位 | 规划参考 Token | 说明 |
| --- | ---: | --- |
| 300M | 约 60 亿 | 本机只适合小规模链路验证或有足够内存的受控实验 |
| 1B | 约 200 亿 | 通常需要更大内存/更强 GPU |
| 3B | 约 600 亿 | 工作站或多 GPU |
| 7B | 约 1400 亿 | 多 GPU/分布式 |
| 14B | 约 2800 亿 | 分布式训练 |
| 38B | 约 7600 亿 | 大型分布式 GPU 集群 |

实际步数必须由 Token、序列长度、Batch、梯度累计和遍历次数计算。少量
样本和几百/几千步只能验证链路，不能承诺模型具备成熟聊天或编程能力。

## 4. 样本规划

- AI 教师样本数表示通过边界、语言、长度、控制字符、完全重复和近重复检查
  的独立样本数，不是 API 请求数，也不是教师自报数量。
- 教师每批立即保存 partial 数据，生成中断、余额不足、断网或服务重启时保留
  已生成内容。
- 训练语料按样本隔离验证集，不能让同一篇文献的重复窗口同时进入训练和验证。
- 人工文献样本数显示为根据 UTF-8 字节量和序列长度计算的估算值；实际训练
  使用的随机窗口可能不同。
- AI 文献/教材/代码是预训练主体；微调根据验证集动态补充专业文献、结构化
  单轮任务、代码/数学、高质量指令对话和身份样本，不固定某个对话比例。

## 5. 参数推荐

训练页保留四种目标：平衡、省时间、省内存、效果优先。每套建议必须同时给出
模型规模、设备、可用内存、样本数、步数、序列长度、Batch、梯度累计、学习率、
Warmup、Weight Decay、梯度裁剪、精度、checkpoint 间隔、预计总时间和峰值内存。

用户可以单独修改参数；手动修改只改变当前结果，不悄悄覆盖四套推荐。修改
步数、序列长度、Batch 或梯度累计后，最终 Token、标准偏差和 ETA 必须实时重算。
点击重置时才重新应用完整的“平衡”配置。

## 6. 内存与时间

- 训练前至少保留 1GB 安全内存；系统内存不足或模型峰值超过可用内存时，
  在分配权重前阻止训练并给出原因。
- 训练中持续监控内存；不足时暂停等待或安全停止，不能让电脑卡死。
- 序列长度、Batch、梯度累计和精度会影响内存/时间；学习率、Warmup、Weight
  Decay 和梯度裁剪主要影响收敛，通常不明显改变单步时间；checkpoint 间隔
  会增加写盘时间。
- 训练开始后使用实测每步耗时计算剩余时间，不能用未更新的预估值长期显示。

## 7. 训练结果边界

“优化器流程完成”“checkpoint 已保存”“loss 下降”和“模型能正常聊天”是
不同状态。必须通过独立聊天/任务验证后才能标记对话质量通过；出现乱码、
无意义字节或答非所问时，不能用程序写死答案掩盖失败。
